// Template for Secrets.cs, which is not tracked in git.
//
// Copy this file to Secrets.cs in the same directory and fill in your own
// Telegram API credentials from https://my.telegram.org -> API development
// tools. The build will not compile without it.
//
// Deliberately in the global namespace and linked into both the phone app and
// the desktop harness: the two projects use different namespaces and there is
// no project reference between them.
internal static class Secrets
{
    public const int ApiId = 0;
    public const string ApiHash = "";
}
