<Page
    x:Class="LumigramPlus.App.ImageViewerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="Black">

    <!--
      A ScrollViewer with zoom enabled does the work: pinch, double-tap to zoom in
      and again to zoom out, and panning once zoomed, all without a line of code.
      Writing gesture handling by hand would be worse in every way.
    -->
    <ScrollViewer x:Name="Zoomer"
                  ZoomMode="Enabled" MinZoomFactor="1" MaxZoomFactor="8"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollMode="Auto" VerticalScrollMode="Auto">
        <Image x:Name="Picture" Stretch="Uniform"/>
    </ScrollViewer>

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:Name="SaveButton" Icon="Save" Label="save" Click="Save_Click"/>
        </CommandBar>
    </Page.BottomAppBar>
</Page>
