<Page
    x:Class="LumigramPlus.App.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LumigramPlus.App"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="16,24,16,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Lumigram+" FontSize="28" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1">
            <TextBlock x:Name="SessionText" Text="" TextWrapping="Wrap" Margin="0,0,0,4"
                       FontSize="20"/>
            <TextBlock x:Name="SessionDetail" Text="" TextWrapping="Wrap" Opacity="0.7"
                       Margin="0,0,0,12"/>

            <!-- Only one of these is ever shown: being offered a sign-in button
                 while already signed in is what made a successful login look like
                 being sent back to the start. -->
            <Button x:Name="SignInButton" Content="sign in with a QR code"
                    Click="SignIn_Click" HorizontalAlignment="Stretch"
                    Visibility="Collapsed"/>
            <Button x:Name="SignOutButton" Content="sign out" Click="SignOut_Click"
                    HorizontalAlignment="Stretch" Visibility="Collapsed"/>

            <Button x:Name="CoreButton" Content="test the connection"
                    Click="Core_Click" HorizontalAlignment="Stretch" Margin="0,8,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" Margin="0,12,0,0">
            <TextBlock x:Name="LogText" Text="" TextWrapping="Wrap" FontSize="15"
                       FontFamily="Consolas"/>
        </ScrollViewer>
    </Grid>
</Page>
