<phone:PhoneApplicationPage
    x:Class="Lumigram.Phone.ImageViewerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="PortraitOrLandscape" Orientation="Portrait"
    shell:SystemTray.IsVisible="False">

    <Grid x:Name="LayoutRoot" Background="Black">

        <!-- Clipped so a zoomed image cannot paint over the status area. -->
        <Grid x:Name="Viewport" Background="Black"
              ManipulationDelta="Viewport_ManipulationDelta"
              ManipulationCompleted="Viewport_ManipulationCompleted"
              DoubleTap="Viewport_DoubleTap"
              Hold="Viewport_Hold">
            <Image x:Name="Zoomed" Stretch="Uniform" RenderTransformOrigin="0,0">
                <Image.RenderTransform>
                    <CompositeTransform x:Name="Transform"/>
                </Image.RenderTransform>
            </Image>
        </Grid>

        <TextBlock x:Name="HintText" VerticalAlignment="Bottom" HorizontalAlignment="Center"
                   Margin="0,0,0,24" Opacity="0.65" TextWrapping="Wrap"
                   Style="{StaticResource PhoneTextSmallStyle}"
                   Text="pinch or double-tap to zoom - hold to save"/>

        <TextBlock x:Name="StatusText" VerticalAlignment="Top" HorizontalAlignment="Center"
                   Margin="12,24,12,0" TextWrapping="Wrap"
                   Foreground="{StaticResource PhoneAccentBrush}"
                   Style="{StaticResource PhoneTextNormalStyle}"/>
    </Grid>

</phone:PhoneApplicationPage>
