<phone:PhoneApplicationPage
    x:Class="Lumigram.Phone.QrLoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="12,17,0,12">
            <TextBlock Text="Lumigram" Style="{StaticResource PhoneTextNormalStyle}"/>
            <TextBlock Text="scan to sign in" Margin="9,-7,0,0"
                       Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Margin="12,0,12,8" TextWrapping="Wrap" Opacity="0.7"
                   Style="{StaticResource PhoneTextSmallStyle}"
                   Text="On another device already signed in to Telegram, open Settings &gt; Devices &gt; Link Desktop Device, then scan this code."/>

        <Border Grid.Row="2" Background="White" HorizontalAlignment="Center"
                VerticalAlignment="Center" Padding="10">
            <Image x:Name="QrImage" Width="300" Height="300" Stretch="None"/>
        </Border>

        <StackPanel Grid.Row="3" Margin="12,8,12,12">
            <ProgressBar x:Name="Busy" IsIndeterminate="True"/>
            <TextBlock x:Name="StatusText" TextWrapping="Wrap"
                       Style="{StaticResource PhoneTextSmallStyle}"/>
            <TextBlock x:Name="DiagText" TextWrapping="Wrap" Opacity="0.5"
                       Style="{StaticResource PhoneTextSmallStyle}"/>
            <Button x:Name="CheckButton" Content="I have scanned it" Click="Check_Click"/>
            <Button x:Name="UseCodeButton" Content="use a phone number instead"
                    Click="UseCode_Click"/>
        </StackPanel>
    </Grid>

</phone:PhoneApplicationPage>
