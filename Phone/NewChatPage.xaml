<phone:PhoneApplicationPage
    x:Class="Lumigram.Phone.NewChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="12,17,0,20">
            <TextBlock Text="Lumigram"
                       Foreground="{StaticResource PhoneForegroundBrush}"
                       Style="{StaticResource PhoneTextNormalStyle}"/>
            <TextBlock Text="new chat" Margin="9,-7,0,0"
                       Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="12,0,12,0">
            <TextBlock TextWrapping="Wrap"
                       Foreground="{StaticResource PhoneForegroundBrush}"
                       Style="{StaticResource PhoneTextNormalStyle}"
                       Text="Enter a username or a phone number."/>
            <TextBox x:Name="QueryBox" Margin="0,8,0,0" KeyDown="QueryBox_KeyDown"/>
            <TextBlock x:Name="KindHint" Margin="12,-4,0,0"
                       Foreground="{StaticResource PhoneForegroundBrush}"
                       Style="{StaticResource PhoneTextSmallStyle}"/>
            <Button x:Name="FindButton" Content="find" Click="Find_Click"/>

            <ProgressBar x:Name="Busy" IsIndeterminate="True" Visibility="Collapsed"/>
            <TextBlock x:Name="StatusText" TextWrapping="Wrap" Margin="12,4,0,0"
                       Foreground="{StaticResource PhoneAccentBrush}"
                       Style="{StaticResource PhoneTextSmallStyle}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" x:Name="ResultPanel" Visibility="Collapsed"
                    Margin="12,24,12,0">
            <TextBlock x:Name="ResultTitle" TextWrapping="Wrap"
                       Foreground="{StaticResource PhoneForegroundBrush}"
                       Style="{StaticResource PhoneTextTitle2Style}"/>
            <TextBlock x:Name="ResultDetail" TextWrapping="Wrap"
                       Foreground="{StaticResource PhoneForegroundBrush}"
                       Style="{StaticResource PhoneTextNormalStyle}"/>
            <Button Content="open chat" Click="Open_Click" Margin="0,12,0,0"/>
        </StackPanel>
    </Grid>

</phone:PhoneApplicationPage>
